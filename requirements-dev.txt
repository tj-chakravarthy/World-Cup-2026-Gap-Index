# What the CI test job needs. scikit-learn is included so the mandatory leakage guard (and the
# other sklearn-backed unit tests, all synthetic-data) run in CI — the mandatory suites hard-fail
# rather than skip if a dep is missing. The genuinely heavy stack (pymc, socceraction, xgboost)
# stays in requirements.txt / the Docker image; only the tests needing it (e.g. test_vaep) skip in CI.
pytest>=8.0.0
numpy>=1.26.0,<2.0
scipy>=1.12.0
pandas>=2.0.0,<3.1   # cap the major: an unpinned 3.x/4.0 bump can silently redden CI (3.0 did)
pyarrow>=14.0.0
scikit-learn>=1.4,<1.6

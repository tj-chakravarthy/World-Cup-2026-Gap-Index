# What the CI test job needs. scikit-learn is here so the MANDATORY leakage guard (and the
# other sklearn-backed unit tests) actually run in CI — they use synthetic data, no scrape.
# The genuinely heavy stack (pymc, socceraction, xgboost) stays in requirements.txt / the
# Docker image; the tests that need it skip in CI.
pytest>=8.0.0
numpy>=1.26.0,<2.0
scipy>=1.12.0
pandas>=2.0.0
pyarrow>=14.0.0
scikit-learn>=1.4,<1.6

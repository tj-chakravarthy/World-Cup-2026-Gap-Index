# pip dependencies — ONLY what Debian doesn't package. The scientific/scraping
# base (pandas, numpy, scipy, scikit-learn, xgboost, matplotlib, requests, ...)
# comes from apt; see apt-packages.txt. Install the gap on top of the apt base:
#   pip install --break-system-packages -r requirements.txt    # or inside a venv
#
# numpy / scikit-learn are intentionally NOT pinned here — apt provides them
# (numpy 2.x, sklearn 1.4.2, which already satisfies mapie 0.8.6's <1.6 need).
# If a lib below turns out to require numpy<2, pin it in a --system-site-packages
# venv so it shadows the apt numpy; verifiera när stacken faktiskt installeras.

soccerdata>=1.9.0
statsbombpy>=1.3.0
socceraction>=1.5.3
pyarrow>=14.0.0
mapie==0.8.6        # PINNED — verify import paths; needs sklearn<1.6 (apt gives 1.4.2)
pymc>=5.0.0
lifelines>=0.28.0
shap>=0.44.0
